*.bak
.claude/*
# Конфигурация проекта хранится в репозитории: без неё на новой машине
# теряются скиллы, агенты и автозагрузка контекста в новом чате.
!.claude/skills/
!.claude/agents/
!.claude/settings.json
!.claude/launch.json
# settings.local.json НЕ коммитим: там локальные пути и личные разрешения
.claude/settings.local.json
.env
.DS_Store

# Исходники картинок — не нужны на сайте, лежат локально
hero-cyborg.png
САЙТ /
# то же имя в NFD-нормализации macOS — иначе шаблон выше не ловит папку
САЙТ*
*/Gemini_Generated_Image_*
.telegram-posted.json

# Новостной конвейер: сырые новости, дайджесты, черновики постов и сценариев.
# Рабочие данные, на сайт не выкладываются.
.news/

# временная папка: device_bash не умеет удалять файлы, владелец удаляет вручную
_to_delete/

# папка с исходниками картинок; имя в NFD-нормализации macOS
САЙТ /
