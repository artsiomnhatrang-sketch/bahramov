## Где остановились

**Обновлено:** 2026-08-23 (вечер, +07) · сайт: 40 статей + 3 страницы услуг

Последняя сессия: соцсети в Search Console, автозагрузка контекста в новый чат,
скиллы на повторяющиеся задачи, техпроверки. Статьи не писались.

- **Instagram `@bahram.av` подключён к Search Console** как отдельный ресурс
  (`sc-creator-profile:instagram.com/bahram.av`). Данные из Google Поиска
  и Discover подтягиваются ~48 часов — **посмотреть после 25.08.2026**.
- **YouTube не подключён.** Под `artsiomnhatrang@gmail.com` виден только канал
  `@bahramovartem` (2 подписчика). Рабочий `@bahramovai` (199 подписчиков,
  261 видео) — на другой почте, Google его не показывает. Владелец решил
  отложить. В `sameAs` и `llms.txt` по его решению оставлен `@bahramovai`.
- **Разметка соцпрофилей исправлена:** Instagram добавлен в `sameAs` (его там
  не было вовсе), ник везде заменён с `@bahramovartem` на `@bahram.av`,
  в Organization добавлен LinkedIn, Instagram добавлен в `llms.txt`.
- **Контекст теперь подаётся в новый чат сам** — хук SessionStart вызывает
  `scripts/session-context.sh`. Пересказывать историю больше не нужно.
  Появились скиллы `/bahramovai-finish`, `/bahramovai-ship`, `/bahramovai-seo`.
  Папка `.claude/` выведена из gitignore, кроме `settings.local.json`.
- **Даты в JSON-LD на всех 40 статьях приведены к ISO 8601** с временем и
  таймзоной (+07:00) — Google помечал прежний формат как недопустимый.
- **Rich Results пройден:** главная и hub-статья без ошибок. FAQPage там не
  появляется — Google с 2023 сузил FAQ-сниппеты, это не поломка разметки.
- **Оптимизация картинок закрыта окончательно:** замер показал выигрыш
  в 18 байт на файл, тема исчерпана.

## Ближайшие задачи

1. **~25.08** — проверить, появились ли данные по Instagram-ресурсу в Search Console.
2. **~06.09** — замер эффекта SEO-правок: `/bahramovai-seo`. Сравнить позиции по
   «регистрация в инстаграм 2026» (была 8.1), «chatplace» (10.0),
   «2 инстаграма на одном телефоне» (6.7).
3. **Риск под наблюдением:** у `instagram-novyy-akkaunt-blokirovka` сменён H1
   у страницы, которая уже ранжировалась. Станет хуже — откатить из git.
4. **Самый жирный незакрытый кластер:** вход в Instagram из России + VPN/прокси,
   415 показов в месяц, своей страницы нет. Писать только через угол «как смена
   IP влияет на риск блокировки» — реклама средств обхода в РФ запрещена.
5. **Очередь статей:** «AI-агент для Instagram и мессенджеров: автоматизация без бана».

## Что заблокировано и почему

- **YouTube в Search Console** — нужен вход в почту, где заведён `@bahramovai`.
  Пароли вводит только Артём.
- **Яндекс.Вебмастер через браузер** — домен заблокирован политикой. Работать
  только через API: `scripts/yandex-stats.py`, `scripts/yandex-recrawl.py`.
- **Сжатие photo.jpg** — нужен cwebp/jpegoptim, Homebrew не установлен.
- **Новостной конвейер остановлен** по просьбе владельца: облачная routine
  `trig_01DCcmbWjEVdCKxZ3P4aSMoa` в состоянии `enabled: false`.
