# STATUS — состояние проекта bahramovai.com

**Обновлено:** 2026-07-18 (+07)
**Всего статей опубликовано:** 33 (по CLAUDE.md; в этой сессии новых не добавляли — переписывали llms.txt)
**Git:** `main`. **Незакоммичено:** `llms.txt` — переписан, ждёт одобрения диффа Артёмом (НЕ пушить без ОК). STATUS.md коммитится отдельно по SESSION-END.

## Последние 3 статьи
| # | Файл | H1 | Дата |
|---|------|-----|------|
| 33 | instagram-bezopasnyy-zapusk-posle-blokirovki-2026.html | Безопасный запуск Instagram после блокировки | 2026-07-16 |
| 32 | telegram-akkaunt-ugon-fishing-2026.html | Как в 2026 угоняют аккаунты в Telegram: актуальные схемы фишинга и как защититься | 2026-07-05 |
| 31 | telegram-vs-max-biznes-2026.html | Telegram или MAX: куда переносить бизнес в 2026 | 2026-07-04 |

## Последнее изменение (эта сессия)
- **2026-07-18: переписан `llms.txt`** под формат «путеводитель для ИИ-агентов» (спека llms.txt 2026). H1 + опорный `>`-блок → Кто я → Услуги (из offer.html) → Кейсы → Статьи (10 реальных ссылок, все проверены `[ -f ]` = OK) → Контакты → Как использовать. Правки: Telegram исправлен (`t.me/bahramovai` → канал `t.me/artsiombahram` + личка `t.me/bahramovartsiom`); добавлены ссылки offer.html/about.html/blog/ и кейс vc.ru. Цифры кейсов — плейсхолдеры `(уточнить)`, кроме «AI-агент заменил 3 менеджеров — экономия 150 000 ₽/мес». **Не закоммичено, ждёт ОК на пуш.**
- 2026-07-17: новых статей не создавали, sitemap не трогали. Три коммита.
  1. **`70a2f8a`** — бэклинк на Т-Бизнес в about.html + запись в CLAUDE.md.
  2. **`6c0704d`** — **новая секция «Публикации»** на about.html (3 живых материала: vc.ru ×2 + Т-Бизнес); внешние материалы вынесены из «Где меня найти», там остались только каналы (LinkedIn, Telegram, Блог, YouTube, Instagram). Секция сделана обычной `.section`, а не `section-alt` — две `alt` подряд склеились бы в один блок. Заголовок `<h2 class="section-title">Публикации</h2>` без `<span>` (одно слово; у остальных заголовков оранжевым подсвечено последнее слово).
  3. **`6c0704d`** — **мёртвая ссылка Sostav вычищена**: `blog/7-signs-you-need-ai-agent.html` ссылался на удалённую статью (404). Заменена на внутреннюю `/blog/ai-agent-vs-chatbot-2026.html` (та же тема, файл проверен, в проде 200). Упоминание Sostav из текста убрано.
  4. **CLAUDE.md причёсан**: раздел «Внешние публикации» переименован в «(выполнено)», протухшие «ждём ссылку»/«дедлайн» убраны, строка про vc.ru приведена к факту (3 живых материала).
- Предыстория (2026-07-16): опубликована №33 (`e092bd2`).

## В работе / не завершено
- **`llms.txt` ждёт одобрения диффа** и пуша (переписан 2026-07-18, см. выше). После ОК — обычный `git add llms.txt && commit && push`.
- **IndexNow не догнал изменённые страницы.** В `6c0704d` менялись `about.html` и `blog/7-signs-you-need-ai-agent.html`, но пинга по ним после этого не было (последний прогон ушёл на `/` — главную, которая не менялась). Скрипт шлёт в IndexNow **ровно один URL из аргумента**. Догнать:
  `./publish-article.sh /about.html` и `./publish-article.sh /blog/7-signs-you-need-ai-agent.html`
- **Анонс в Telegram @artsiombahram** про статью в Т-Бизнесе — НЕ сделан (`scripts/post-to-telegram.sh`). Последний невыполненный пункт по кейсу @eldarmurakov.
- **Внешняя дистрибуция №33 не выполнена**: vc.ru, LinkedIn, Telegram-анонс, GSC. Журнала для №33 в DISTRIBUTION-PLAN.md нет.
- У №33 **ноль входящих** внутренних ссылок — добавить 1–2 донора (кандидаты: №26, №17, №24).

## Очередь тем (TODO из CLAUDE.md)
- Полное руководство по блокировкам Instagram 2026 (hub-страница, линкует кластер — сейчас 14 IG-статей).
- AI-агент для Instagram и мессенджеров: автоматизация без бана.

## Открытые хвосты и техдолг
- **Sostav: статья удалена изданием навсегда** (404, блог автора пуст, проверено 17.07.2026). Ссылку не восстанавливать, материал в списке публикаций помечен ❌. Вопрос закрыт.
- **Две линии шаблонов в блоге**, канон не зафиксирован. «Линия B» (`.article-body` + `.btn-primary` + `:root`-переменные + «🎁 Бонусы») — 16 файлов, весь IG-кластер. «Линия A» (`<main class="article">` + `.cta-button` + хардкод-цвета, без «Бонусов») — 13 файлов, Telegram/мессенджеры. CLAUDE.md каноном называет `train-ai-agent-like-manager.html` (линия B), но свежие статьи писались по линии A. Починить п.1 «ШАБЛОНА» в CLAUDE.md.
- **№33 без FAQPage JSON-LD** — п.10 CLAUDE.md требует три блока; Q&A не сочиняли из-за запрета на выдумывание.
- **№33: слабый анкор** второй кросс-ссылки — «в первые недели».
- CLAUDE.md: строки про BreadcrumbList и JSON-LD говорят «все 27 статей» — устарело (уже 33).
- TODO п.4 в CLAUDE.md содержит уже выполненный кандидат (Telegram vs MAX) — почистить.
- `publish-article.sh`: **«✓ Google sitemap ping OK» — ложный рапорт**, endpoint отключён Google в 2023, код ответа не проверяется. Реально работают только IndexNow (Bing/Yandex) и Bing-пинг. Скрипт рабочий **только из корня** (внутри относительный вызов `scripts/check-stat-cards.py`). Шлёт в IndexNow только один URL — по одному прогону на страницу.
- **Грабли perl**: в `perl -0777 -i -pe` двойные кавычки интерполируют `@handle` как массив — Telegram-хэндлы молча исчезают из текста. Использовать `q{...}`. Наступали на это.
- **Гард «ровно 1 совпадение» и порядок операций**: при переносе блоков сначала удалять, потом вставлять — иначе копия совпадёт с оригиналом и гард честно упадёт на двух совпадениях.
- `hero-cyborg.png` (943 КБ, сирота) — удалить или вынести из репозитория.
- Привязать YouTube-канал к artsiomnhatrang@gmail.com (связка с Google Search Console).
- Проверить FAQPage в Rich Results.
- Тема разбана/целостности Instagram плотно покрыта: №14, №17, №25, №26, №33 — новые статьи по этому углу не плодить, дополнять существующие.

## Правила проекта
Перед статьёй — сверка на дубль (grep + index.html) и проверка существования файлов ссылок (включая внешние — curl на 200); фактчек с атрибуцией, без выдуманных цифр и названий систем Meta; git diff перед пушем (ровно нужные файлы, без полной перезаписи, правки через perl/str_replace с гардом «ровно 1 совпадение»); `publish-article.sh` запускает пользователь вручную.
